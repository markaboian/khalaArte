server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${GIT_REPO_URL}
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}